const TAB_BAR = 'tab-bar';

const TAB_HOME = 'home';
const TAB_BROWSE = 'browse';
const TAB_CART = 'cart';
const TAB_MORE = 'more';
const TAB_NONE = 'none';
const TAB_FAVORITES = 'favorites';

const HOME_ICON = `${TAB_HOME}-icon`;
const BROWSE_ICON = `${TAB_BROWSE}-icon`;
const CART_ICON = `${TAB_CART}-icon`;
const MORE_ICON = `${TAB_MORE}-icon`;
const FAVORITES_ICON = `${TAB_FAVORITES}-icon`;

const BEFORE = 'before';
const AFTER = 'after';

export const TAB_BAR_HOME_BEFORE = `${TAB_BAR}.${TAB_HOME}.${BEFORE}`;
export const TAB_BAR_HOME = `${TAB_BAR}.${TAB_HOME}`;
export const TAB_BAR_HOME_AFTER = `${TAB_BAR}.${TAB_HOME}.${AFTER}`;

export const TAB_BAR_BROWSE_BEFORE = `${TAB_BAR}.${TAB_BROWSE}.${BEFORE}`;
export const TAB_BAR_BROWSE = `${TAB_BAR}.${TAB_BROWSE}`;
export const TAB_BAR_BROWSE_AFTER = `${TAB_BAR}.${TAB_BROWSE}.${AFTER}`;

export const TAB_BAR_CART_BEFORE = `${TAB_BAR}.${TAB_CART}.${BEFORE}`;
export const TAB_BAR_CART = `${TAB_BAR}.${TAB_CART}`;
export const TAB_BAR_CART_AFTER = `${TAB_BAR}.${TAB_CART}.${AFTER}`;

export const TAB_BAR_MORE_BEFORE = `${TAB_BAR}.${TAB_MORE}.${BEFORE}`;
export const TAB_BAR_MORE = `${TAB_BAR}.${TAB_MORE}`;
export const TAB_BAR_MORE_AFTER = `${TAB_BAR}.${TAB_MORE}.${AFTER}`;

export const TAB_BAR_FAVORITES_BEFORE = `${TAB_BAR}.${TAB_FAVORITES}.${BEFORE}`;
export const TAB_BAR_FAVORITES = `${TAB_BAR}.${TAB_FAVORITES}`;
export const TAB_BAR_FAVORITES_AFTER = `${TAB_BAR}.${TAB_FAVORITES}.${AFTER}`;
