import { css } from 'glamor';

const removeArea = css({
  backgroundColor: '#ff3b30',
  width: '100px',
  height: '100%',
  position: 'absolute',
  right: 0,
  top: 0,
}).toString();

export default {
  removeArea,
};
